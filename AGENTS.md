# Andrey AI Lab — память проекта

Файл для сохранения контекста разработки. При возобновлении работы прочитайте этот файл перед внесением изменений.

---

## О проекте

Личный сайт фрилансера Андрея Семенова, который создаёт ИИ-ассистентов и чат-ботов для бизнеса.

Позиционирование: «Андрей Семенов — ИИ-ассистенты для бизнеса».

### Ссылка на Telegram

`https://t.me/a7onoff72` (вынесена в `config.py`).

---

## Технический стек

- **Backend:** Python 3.10+, Flask 3.0
- **База данных:** SQLite через Flask-SQLAlchemy
- **Авторизация:** Flask-Login
- **Формы и CSRF:** Flask-WTF + CSRFProtect
- **Шаблоны:** Jinja2
- **Стили:** чистый CSS, без Bootstrap
- **Шрифты:** Inter (основной текст) + JetBrains Mono (номера, метки, код)

### Запуск

```bash
cd andrey-ai-lab
pip install -r requirements.txt
python app.py
```

Сайт: `http://127.0.0.1:5000/`  
Админ-панель: `http://127.0.0.1:5000/admin/`

Логин/пароль администратора задаются в `config.py`.

---

## Дизайн-принципы (стиль Oblivion)

- Светлый фон `#FAFAFA`, тёмный текст `#111111`
- Много воздуха, крупные отступы, тонкие разделительные линии 1px `#E5E5E5`
- Секции с номерами `01`, `02`, `03` моноширинным шрифтом
- Белые скруглённые карточки (~24px), мягкая тень
- Серые pill-теги
- Крупная типографика, лёгкий weight
- Деликатные hover-эффекты, без кричащих анимаций
- Адаптивная вёрстка

---

## История разработки

### v1.0 — базовый сайт

Создан каркас Flask-приложения:
- `app.py` с моделями, формами, роутами и логированием
- `config.py` с настройками, админом и Telegram
- Главная страница с hero, пользой для бизнеса, кейсами, этапами работы
- Страница кейсов + 5 отдельных страниц кейсов
- Страница «Обо мне»
- Форма обратной связи с валидацией и CSRF
- Админ-панель: вход, просмотр заявок, отметка прочитано, удаление
- README.md с инструкциями

### v1.1 — иконка сайта

Добавлен логотип из файла:
`C:/Users/Andrey/Desktop/Zerocoder/Проекты/Project images/ChatGPT Image 20 авг. 2026 г., 20_26_39.png`

Созданы версии:
- `static/images/logo.png` — оригинал
- `static/images/logo-small.png` — 48×48 для шапки/подвала
- `static/images/favicon.png` — 32×32
- `static/images/apple-touch-icon.png` — 180×180

Логотип используется:
- favicon на всех страницах
- в шапке вместо текстового маркера «AA"
- в подвале

### v1.2 — гравитационный фон

Добавлен фон с плавающими фигурами и фрагментами кода:
- размытые круги, линии, точки
- 13 фрагментов кода моноширинным шрифтом
- медленное плавающее движение по всей высоте сайта
- лёгкий параллакс по движению мыши (отключён на тач-устройствах)
- фон растянут на всю высоту документа через JS

Параметры контраста:
- непрозрачность фигур: `0.55`
- цвет кода: `rgba(120, 120, 120, 0.32)`
- амплитуда движения: `translate(36px, -48px)`

### v1.3 — SEO и соцсети

Добавлены SEO-элементы:
- Уникальные `title` и `meta description` для каждой страницы
- Open Graph и Twitter Card мета-теги
- Canonical URL
- JSON-LD разметка (`ProfessionalService` + `Person`)
- `robots.txt` с закрытием админки от индексации
- `sitemap.xml` со списком всех страниц и кейсов
- OG-изображение `static/images/og-image.png` (1200×630) для превью ссылок

### v1.4 — плавающий код «рыбки в аквариуме»

Фоновые фрагменты кода теперь движутся как рыбки:
- 24 фрагмента кода (~12 видимых в начальном viewport)
- 6 разных keyframe-анимаций (`swim-right-1/2/3`, `swim-left-1/2/3`)
- Каждый фрагмент плывёт по своей изогнутой траектории
- Плавное появление и исчезновение по краям (opacity 0 → 0.42 → 0)
- Небольшой поворот в «плавниках» для естественности
- Длительность анимаций 36–54 секунд
- Контраст кода увеличен до `rgba(110, 110, 110, 0.42)`

### v1.5 — кейс RAG-ассистента, политика конфиденциальности, фото

- Добавлен 6-й кейс «RAG-ассистент с логированием и метриками» (`slug: rag-assistant-logging`),
  в `details` кликабельная ссылка на Telegram-бота с эмодзи 👉 (класс `.bot-link`, синий цвет,
  вывод через `| safe` в `case_detail.html`), создана SVG-заглушка `case-logging.svg`
  (шрифт JetBrains Mono, как у остальных кейсов)
- Добавлена страница «Политика конфиденциальности» (`/privacy/`, `templates/privacy.html`)
  по структуре 152-ФЗ; ссылка в подвале, страница в sitemap.xml
- В форму обратной связи добавлен чекбокс согласия на обработку персональных данных
  (без него форма не отправляется)
- На страницу «Обо мне» добавлено фото владельца (`static/images/photo-andrey.jpg`,
  обрезано снизу без водяного знака, стили `.about__photo`)
- Telegram-аккаунт владельца изменён на @a7onoff72 (`config.py`, политика)
- Добавлен `.gitignore` (`__pycache__/`, `logs/`, `.idea/`, `.venv/`),
  служебные файлы удалены из репозитория

---

## Текущий функционал

- Главная страница с hero и CTA
- 6 кейсов с отдельными страницами (включая RAG-ассистент с логированием)
- 5 тезисов «Польза для бизнеса»
- 3 шага «Как я работаю»
- Форма обратной связи с сохранением в БД и чекбоксом согласия на обработку ПДн
- Админ-панель с авторизацией
- Логирование в `logs/app.log`
- Гравитационный фон
- Иконка/логотип на всех страницах
- SEO: Open Graph, Twitter Card, canonical, JSON-LD, robots.txt, sitemap.xml
- Адаптивная вёрстка
- Возможность добавлять кейсы по ссылке на публичный GitHub-репозиторий

---

## План ближайших работ (при возобновлении начать отсюда)

### Технический долг

1. **Дописать плейсхолдеры в `templates/privacy.html`** — отчество, статус
   (ИП/самозанятый), ОГРНИП/ИНН, email для запросов о ПДн (помечены как `[...]`)
2. **Убрать `instance/site.db` из git** — добавить в `.gitignore`, удалить из
   репозитория (`git rm --cached`), база с заявками не должна попадать в коммиты

### Развитие сайта (по приоритету)

3. **Страница «Услуги / Цены»** — перечень услуг с ценовыми диапазонами
4. **Уведомления о заявках в Telegram** — при новой заявке из формы отправлять
   сообщение админу через Telegram Bot API (токен вынести в `config.py` /
   переменные окружения)
5. **FAQ** — 5–7 вопросов: стоимость, сроки, нужны ли данные, интеграции, ошибки
6. **Живой демо-бот на сайте** — виджет в углу экрана (самая трудоёмкая фича)

### Отложено

- **Деплой на хостинг в РФ** — сайт собирает ПДн (форма), по 152-ФЗ база
  должна храниться в России. GitHub Pages и зарубежные VPS не подходят.
  Кандидаты: Timeweb, reg.ru, Selectel, Yandex Cloud

---

## Важные решения и заметки

- Telegram-аккаунт владельца: **@a7onoff72** (`https://t.me/a7onoff72`, в `config.py`).
  Старый @semandr72 не используется. GitHub-username `semandr72-eng` — отдельный,
  в ссылках на репозитории кейсов оставить как есть.
- Фото владельца: `static/images/photo-andrey.jpg` (обрезано снизу, без водяного знака),
  выводится на странице «Обо мне» над карточкой «25+».
- В политике конфиденциальности указано, что данные хранятся на серверах в РФ —
  при выборе хостинга это обязательное условие.
- После изменений `app.py` и `config.py` приложение нужно перезапускать.

---

## Как добавлять кейсы

Кейсы хранятся в Python-списке `CASES` в файле `app.py`. Каждый кейс — словарь с полями:

```python
{
    'slug': 'уникальный-идентификатор',
    'number': '06',
    'title': 'Название кейса',
    'short': 'Краткое описание для карточки',
    'tags': ['Tag1', 'Tag2'],
    'github': 'https://github.com/...',
    'image': 'case-slug.svg',
    'details': 'Подробное описание проекта',
}
```

### Добавление кейса по ссылке на GitHub

AI-ассистент может добавить кейс по ссылке на публичный репозиторий:

1. Пользователь присылает ссылку, например:
   `https://github.com/semandr72-eng/ai-hr--company-assistant`
2. Ассистент читает README и структуру репозитория
3. Составляет заголовок, краткое и подробное описание
4. Определяет технологии по коду (FastAPI, ChromaDB, LangChain и т.д.)
5. Добавляет новый словарь в `CASES` в `app.py`
6. Создаёт SVG-заглушку в `static/images/` или предлагает загрузить картинку
7. Проверяет, что страница `/cases/<slug>/` открывается
8. Sitemap обновляется автоматически

### Ограничения

- Работает только с публичными репозиториями
- Сложную бизнес-логику лучше объяснить парой фраз
- После изменений `app.py` нужно перезапустить приложение

---

## Идеи на будущее

### Быстрые улучшения

1. **SEO и соцсети** ✅ (реализовано в v1.3)
   - Open Graph и Twitter Card мета-теги
   - `robots.txt` и `sitemap.xml`
   - JSON-LD разметка (`Person`, `ProfessionalService`)

2. **Микроанимации при скролле**
   - Плавное появление секций по мере прокрутки
   - Реализовать через CSS + Intersection Observer

3. **Страница «Услуги / Цены»**
   - Перечень услуг с ценовыми диапазонами

### Средние по трудоёмкости

4. **Калькулятор стоимости**
   - Форма с выбором типа бота, объёма данных, канала
   - Вывод примерной цены

5. **Отзывы / кейсы-клиенты**
   - 2–3 отзыва с фото/логотипами
   - Блок на главной или отдельная страница

6. **FAQ**
   - 5–7 частых вопросов о стоимости, данных, интеграции, ошибках

7. **Блог / полезные материалы**
   - Статьи про RAG, LLM, автоматизацию HR/поддержки
   - Нужны для SEO и позиционирования эксперта

### Более серьёзные фичи

8. **Живой демо-бот на сайте**
   - Виджет в углу экрана для вопросов по кейсам
   - Демонстрирует продукт в действии

9. **Автоматические уведомления о заявках**
   - Email или Telegram при новой заявке

10. **Расширенная админ-панель**
    - Статистика заявок за период
    - Экспорт в CSV
    - Фильтрация по статусу и теме

### Визуальные и UX-идеи

11. **Тёмная тема**
    - Переключатель светлая/тёмная
    - Хранение выбора в `localStorage`

12. **Анимированный hero**
    - Typing-effect для подзаголовка
    - Или медленное мерцание ключевых слов

13. **Фильтр кейсов по технологиям**
    - Кнопки: RAG, Telegram, HR, Finance и т.д.

14. **Страница политики конфиденциальности**
    - Необходима при сборе персональных данных

---

## Важные замечания

- В production обязательно изменить `SECRET_KEY`, `ADMIN_USERNAME`, `ADMIN_PASSWORD`
- Логин и пароль администратора хранятся в `config.py`
- База данных SQLite создаётся автоматически при первом запуске
- Все чувствительные настройки лучше выносить в переменные окружения

---

## Структура файлов

```
andrey-ai-lab/
├── app.py
├── config.py
├── requirements.txt
├── README.md
├── AGENTS.md            # этот файл
├── instance/site.db     # SQLite-база
├── logs/app.log         # логи
├── static/
│   ├── css/style.css
│   ├── js/main.js
│   └── images/
│       ├── logo.png
│       ├── logo-small.png
│       ├── favicon.png
│       ├── apple-touch-icon.png
│       └── case-*.svg
└── templates/
    ├── base.html
    ├── index.html
    ├── cases.html
    ├── case_detail.html
    ├── about.html
    ├── contact.html
    ├── errors/404.html
    └── admin/
        ├── login.html
        └── dashboard.html
```
